// Not a fluid kernel — write Grid → RT for post-process / debug.
// Include: StageExportRT.ush only

void SF_ExportRT(
	float2 PrevVelocity,
	float PrevDensity,
	out float4 OutColor)
{
	float d = max(PrevDensity, 0.0);
	OutColor = float4(PrevVelocity.x, PrevVelocity.y, d, d);
}
